{
  "name": "nutc-food-picker",
  "version": "1.0.0",
  "description": "A food picker app for NUTC students.",
  "main": "index.html",
  "scripts": {
    "start": "serve",
    "build:ssg": "node scripts/generate-ssg-pages.js",
    "build": "npm run build:ssg",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
